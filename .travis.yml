language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-atom-shell' ]; then cd .. && eval "mv $currentfolder generator-atom-shell" && cd generator-atom-shell; fi

