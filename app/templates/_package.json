{
  "name": "<%= appname %>",
  "version": "<%= version %>",
  "devDependencies": {},
  "scripts": {
    "postinstall": "bower install && node node_modules/gulp/bin/gulp.js atom-download && node node_modules/gulp/bin/gulp.js default"
  }
}
